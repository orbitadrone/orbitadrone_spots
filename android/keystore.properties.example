# Copy this file to android/keystore.properties (which is gitignored) and
# replace the placeholder values with the credentials of your release keystore.
# You can also provide the same properties via environment variables or
# `./gradlew -PORBITA_KEYSTORE_PASSWORD=...`.

ORBITA_KEYSTORE_FILE=/absolute/path/to/orbitadrone_release.keystore
ORBITA_KEYSTORE_PASSWORD=your-keystore-password
ORBITA_KEY_ALIAS=your-key-alias
ORBITA_KEY_PASSWORD=your-key-password
